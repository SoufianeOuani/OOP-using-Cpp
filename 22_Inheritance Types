
## 1️⃣ الوراثة الفردية (**Single Inheritance**)

* صنف واحد يرث من صنف واحد آخر.
* أبسط شكل للوراثة.

```cpp
class A { };
class B : public A { }; // B يرث من A
```

📌 **تصور:**

```
A
│
B
```

---

## 2️⃣ الوراثة متعددة المستويات (**Multi-Level Inheritance**)

* الوراثة تكون على عدة مستويات، حيث يرث صنف من صنف، ثم يرث صنف آخر من هذا المشتق.

```cpp
class A { };
class B : public A { };
class C : public B { }; // C يرث من B، وB يرث من A
```

📌 **تصور:**

```
A
│
B
│
C
```

---

## 3️⃣ الوراثة الهرمية (**Hierarchical Inheritance**)

* عدة أصناف ترث من صنف أساسي واحد.

```cpp
class A { };
class B : public A { };
class C : public A { };
```

📌 **تصور:**

```
   A
  / \
 B   C
```

---

## 4️⃣ الوراثة المتعددة (**Multiple Inheritance**)

* صنف واحد يرث من أكثر من صنف أساسي.

```cpp
class A { };
class B { };
class C : public A, public B { }; // C يرث من A وB
```

📌 **تصور:**

```
A     B
 \   /
   C
```

---

## 5️⃣ الوراثة الهجينة (**Hybrid Inheritance**)

* مزيج من أكثر من نوع وراثة، مثل الجمع بين **Multiple** و **Multi-Level** أو **Hierarchical**.
* غالبًا يستخدم في الحالات المعقدة، وقد يسبب **Diamond Problem** إذا لم يُستخدم `virtual` inheritance.

```cpp
class A { };
class B : public A { };
class C : public A { };
class D : public B, public C { }; // مثال على Diamond Problem
```

📌 **تصور:**

```
    A
   / \
  B   C
   \ /
    D
```
