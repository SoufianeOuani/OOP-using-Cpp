
## 1️⃣ الوراثة العامة (**Public Inheritance**)

```cpp
class Derived : public Base { ... };
```

* **Public** في الـ Base → يبقى **Public** في الـ Derived.
* **Protected** في الـ Base → يبقى **Protected** في الـ Derived.
* **Private** في الـ Base → غير مرئي في الـ Derived.

💡 **الخلاصة:**
تحافظ على نفس الصلاحيات، وهي الأكثر استخدامًا في تصميم الواجهات.

---

## 2️⃣ الوراثة المحمية (**Protected Inheritance**)

```cpp
class Derived : protected Base { ... };
```

* **Public** في الـ Base → يصبح **Protected** في الـ Derived.
* **Protected** في الـ Base → يبقى **Protected** في الـ Derived.
* **Private** في الـ Base → غير مرئي في الـ Derived.

💡 **الخلاصة:**
الأعضاء الموروثة يمكن للأبناء والأحفاد الوصول إليها، لكن الكائنات من الخارج لا تستطيع.

---

## 3️⃣ الوراثة الخاصة (**Private Inheritance**)

```cpp
class Derived : private Base { ... };
```

* **Public** في الـ Base → يصبح **Private** في الـ Derived.
* **Protected** في الـ Base → يصبح **Private** في الـ Derived.
* **Private** في الـ Base → غير مرئي في الـ Derived.

💡 **الخلاصة:**
الأعضاء الموروثة تصبح خاصة في المشتق، فلا يراها أي صنف مشتق لاحقًا ولا أي كائن من الخارج.

---

## 📊 جدول مقارنة سريع:

| نوع الوراثة   | Public في Base → | Protected في Base → | Private في Base → |
| ------------- | ---------------- | ------------------- | ----------------- |
| **Public**    | Public           | Protected           | غير مرئي          |
| **Protected** | Protected        | Protected           | غير مرئي          |
| **Private**   | Private          | Private             | غير مرئي          |
